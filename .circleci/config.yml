version: 2.1

jobs:

  ruby-linters:
    name: Test
    context: global
    docker:
      - image: cimg/ruby:2.7.4
    resource_class: small
    steps:
      - checkout
      - run:
          name: Check SECRET
          command: echo $SECRET
      - run:
          name: Check AWS_REGION
          command: echo $AWS_REGION
